# Khmer Type Master

A modern **Khmer Unicode typing tutor** — progressive lessons from home-row consonants through subscripts, formal writing, and a timed capstone exam.

Practice works offline in the browser. Progress is saved in `localStorage` (cloud sync with a KruMath account is planned later).

## Features

- **16-level curriculum** from basic NiDA keys to advanced text and timed assessment
- Interactive typing surface with WPM / CPM, accuracy, and mistake tracking
- On-screen Khmer keyboard + finger guidance
- Soft typing feedback sounds (project-owned samples)
- Soft-gated for [krumath.com](https://krumath.com): anyone can practice; signed-in-only actions can be added later

## Tech stack

- [TanStack Start](https://tanstack.com/start) + React 19
- Vite 8, Tailwind CSS 4
- [khmer-segment](https://www.npmjs.com/package/khmer-segment) for Khmer Unicode helpers
- Cloudflare Worker deploy via Nitro (`cloudflare-module`)

## Quick start

Requirements: Node.js 20+ and npm.

```sh
git clone https://github.com/sokna492-km/khmer-type-master.git
cd khmer-type-master
npm install
npm run dev
```

App runs under the Vite base path:

```text
http://localhost:5173/khmer-typing-master/
```

### Scripts

| Command | Description |
|---------|-------------|
| `npm run dev` | Local development server |
| `npm run build` | Production build |
| `npm run preview` | Preview the production build |
| `npm test` | Run Vitest unit/component tests |
| `npm run lint` | ESLint |
| `npm run sounds` | Regenerate WAVs in `public/sounds/` |
| `npm run deploy` | Build + deploy Cloudflare Worker (Nitro) |

### Environment

Copy [`.env.example`](.env.example) to `.env` when wiring KruMath Supabase SSO:

- `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY` — same project as KruMath
- `VITE_KRUMATH_ORIGIN` (optional) — local KruMath origin for sign-in/home redirects

Never commit real secrets. `VITE_*` values are embedded at **build** time.

## Sounds

Feedback WAVs under `public/sounds/` are generated by [`scripts/generate-typing-sounds.mjs`](scripts/generate-typing-sounds.mjs) (MIT, no third-party samples):

```sh
npm run sounds
```

## KruMath integration

This app is designed as a **separate repo** mounted at:

```text
https://krumath.com/khmer-typing-master
```

See [`KRUMATH_GAME_INTEGRATION.md`](KRUMATH_GAME_INTEGRATION.md) for the full pattern (soft gate, Cloudflare route, operator checklist). Do **not** edit the KruMath monorepo from this repository.

### Operator checklist (after deploy)

1. Deploy Worker (`npm run deploy`) with Supabase env present at build time
2. Cloudflare route: `krumath.com/khmer-typing-master*` → Worker `khmer-typing-master`
3. Smoke-test assets under `/khmer-typing-master/assets/...`
4. Maintainer (separate PR): add `/khmer-typing-master` to returnUrl allowlist + `/home` card

## License

[MIT](LICENSE)

### Credits

- Khmer segmentation helpers: [`khmer-segment`](https://www.npmjs.com/package/khmer-segment)
- NiDA keyboard artwork: Wikimedia Commons layout used in `public/khmer_layout.svg`
